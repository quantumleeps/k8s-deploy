apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-units.fullname" . }}
  labels:
    {{- include "mcp-units.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mcp-units.selectorLabels" . | nindent 4 }}
